# =============================================================================
# 环境变量配置
# =============================================================================
# 这些变量需要在 Vercel Dashboard 中设置
# Settings → Environment Variables
# =============================================================================

# API 基础地址（Vercel 部署后的地址）
# 构建时会替换为实际值
VITE_API_BASE_URL=https://hx-ui.vercel.app

NODE_ENV=production

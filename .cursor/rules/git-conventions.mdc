---
description: Git 提交规范和版本号规则
alwaysApply: true
---

# Git 提交规范

## 提交信息语言
提交说明必须使用**中文**，禁止使用英文。

## 提交信息格式
```
<type>(<scope>): <description>

[可选的正文]
[可选的脚注]
```

- **Header**：必填，不超过 72 字符，格式为 `type(scope): 描述`
- **Body**：可选，说明"为什么做"和"做了什么"
- **Footer**：可选，关闭 issue（`Closes #123`）或标记破坏性变更

## 提交类型

| 类型 | 用途 |
|------|------|
| feat | 新功能 / 新组件 |
| fix | 修复 bug |
| refactor | 重构（不新增功能也不修 bug）|
| style | 样式变更（不影响逻辑）|
| perf | 性能优化 |
| test | 添加或修改测试用例 |
| docs | 文档变更 |
| chore | 不修改 src/test 的杂务（.gitignore、本地脚本等）|
| ci | CI/CD 配置或脚本变更 |
| build | 构建系统或外部依赖变更 |

## scope 规范
- 使用模块名，**kebab-case**（如 `table`、`json-view`）
- 跨多模块的全局变更可不加 scope，或使用 `global`

## 破坏性变更
- Header 类型后加 `!`：`feat!: 移除对 IE 的支持`
- Footer 标注 `BREAKING CHANGE:` 及说明

## 单一职责
一个提交只做一件事。不同模块、不同目的的变更必须拆分为多个独立提交。

## 版本号规则（当前 0.x 阶段）
版本号 `0.MINOR.PATCH`，由**发布提交**负责更新。

| 变更类型 | 示例（当前 0.0.11） | 规则 |
|---------|-------------------|------|
| feat | 0.0.11 → 0.1.0 | 递增 MINOR，PATCH 归零 |
| BREAKING CHANGE | 0.0.11 → 0.1.0 | 递增 MINOR（0.x 内允许）|
| fix | 0.0.11 → 0.0.12 | 递增 PATCH |
| 其他类型（docs/style/test） | 不触发版本变更 | 手动评估 |

0.x 阶段 MINOR 递增即视为可能包含不向下兼容的改动。项目稳定后升至 1.0.0，再严格遵循 Semver。

## 禁止项
- 禁止提交未完成的功能、调试代码、临时文件到共享分支。WIP 可提交到特性分支，合并前需 rebase 整理。
- 禁止 `git push --force` 推送到共享分支。
- 提交前需通过 lint 检查。
